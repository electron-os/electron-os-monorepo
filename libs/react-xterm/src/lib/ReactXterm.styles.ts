import { css } from 'glamor';
import type { IReactXtermStyleProps } from './ReactXterm.types';

// eslint-disable-next-line
export const Style = ({}: IReactXtermStyleProps) => {
  return { root: css({}) };
};

export default Style;
